<table id="accCape" class="table table-bordered agriculture">
    <thead>
    <tr>
        <th colspan="2" class="clean">CAPES</th>
    </tr>
    <tr>
        <th>Pseudo</th>
        <th class="tier">Tier</th>
    </tr>
    </thead>

    <tbody>
    <tr>
        <td colspan="2" class="null"></td>
    </tr>
    {% for personage in personages %}
        {% if personage.accCape %}
            <tr>
                <td class="clean">{{ personage.name }}</td>
                {% if personage.accCape == 3 %}
                    <td class="trois tier">{{ personage.accCape }}</td>
                {% elseif personage.accCape == 4 %}
                    <td class="quatre tier">{{ personage.accCape }}</td>
                {% elseif personage.accCape == 5 %}
                    <td class="cinq tier">{{ personage.accCape }}</td>
                {% elseif personage.accCape == 6 %}
                    <td class="six tier">{{ personage.accCape }}</td>
                {% elseif personage.accCape == 7 %}
                    <td class="sept tier">{{ personage.accCape }}</td>
                {% elseif personage.accCape == 8 %}
                    <td class="huit tier">{{ personage.accCape }}</td>
                {% else %}
                    <td class="deux tier">{{ personage.accCape }}</td>
                {% endif %}
            </tr>
        {% endif %}
    {% endfor %}
    </tbody>
</table>